
# 将输入字符存入数组


思路

1. 定义两个变量，一个作为暂存用户输入值的变量（x），一个作为数组下标值的变量（i）
2. 用这种方式存入字符至数组

    ```c
    scanf("%d", &array[i]);
    ```

3. 数组下标加1
4. 循环2 3步

    示例：


    ```c
    for (i = 0; i < n; i++)
    {
      printf("Element %d: ", i + 1);
      scanf("%d", &array[i]); // 将用户输入的整数存储在数组中
    }
    ```

